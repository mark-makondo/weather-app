{
  "name": "backend",
  "version": "1.0.0",
  "description": "Testing for conversion of API to human readable.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/server.js",
    "dev": "concurrently \"nodemon --watch .env  src/server.js\" \"cd ../client/ &&  npm start\""
  },
  "keywords": [],
  "author": "Mad.dev",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "node-cron": "^3.0.0",
    "node-fetch": "^2.6.1",
    "nodemailer": "^6.6.2",
    "puppeteer": "^10.0.0"
  }
}
